name: board_aid
description: Project for FIT BUT TAMa – Board Aid

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  shared_preferences: ^2.0.13
  flutter:
    sdk: flutter
  flutter_form_builder: ^7.7.0
  dashboard:
    #path: '../dashboard' #leaved for debugging purposes
    git:
      url: https://github.com/xcoupe01/dashboard
  cupertino_icons: ^1.0.4
  flutter_speed_dial: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - 'assets/icon.png'
